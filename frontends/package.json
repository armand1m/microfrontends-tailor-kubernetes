{
  "name": "frontends",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "lerna run --parallel start",
    "start:fragments": "lerna run --parallel start --ignore layout-service",
    "build": "lerna run --parallel build",
    "watch": "lerna run --parallel watch",
    "install": "lerna bootstrap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
